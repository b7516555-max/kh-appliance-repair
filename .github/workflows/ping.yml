name: Daily Health Check

on:
  schedule:
    # 每天 01:00 至 20:00（UTC）每小時執行一次
    - cron: '0 1-20/1 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Website
        run: curl -sI https://b7516555-max.github.io/kh-appliance-repair/

# LINE Rich Menu 六入口與四個前端頁面整合執行報告

## 一、 執行概況

本專案已依「自主執行模式（Autonomous Execution Mode）」及安全守則，在不拆分獨立 Web App 與不破壞既有架構的前提下，於現有 `Code.gs`、`Index.html`、`Line.gs` 中完成 **LINE Rich Menu 全新 6 格 URI Actions 定義**、**4 個公開獨立前端視圖（維修據點、查詢進度、服務須知、常見問題）**以及**雙軌相容路由機制（相容 Hash Routing 與 Query Parameter 路由）**。

- **執行狀態**：`SUCCESS`（全部完成並已驗證生效）
- **正式上線 Rich Menu ID**：`richmenu-e171231fbc0f6015cf8909488c49d55d`
- **正式 Web App 部署版本**：`AKfycbwwutONntwhqljCXcdILF3pdtX6v4Rk74nKSq2j9f_ECmkkfIZ3CZ4eNPLlys5NNrYA @211`
- **正式 Web App URL**：`https://script.google.com/macros/s/AKfycbwwutONntwhqljCXcdILF3pdtX6v4Rk74nKSq2j9f_ECmkkfIZ3CZ4eNPLlys5NNrYA/exec`

---

## 二、 4 個全新公開視圖與前端規格

所有視圖均在同一個 `Index.html` 內以卡片式 Responsive UI 實作，專為手機 LINE WebView 體驗量身最佳化（支援 iPhone Safe Area、無橫向捲動、16px 以上輸入框、大型操作按鈕）：

1. **維修據點 (`view=locations`)**
   - **南鳳山清潔隊**：高雄市鳳山區國泰路一段69巷1號（每月第二、四週三 14:00-17:00），提供 Google Maps 導航與預約捷徑。
   - **楠梓家具展示區**：高雄市楠梓區清豐三路380號（週四、五 09:00-12:00, 13:00-16:00），提供 Google Maps 導航與預約捷徑。
   - **巡迴與非定點活動**：動態從 `getInitialData()` 載入未過期之推廣場次，絕不硬編碼死日期。
2. **查詢進度 (`view=status`)**
   - **安全雙重驗證**：僅需「案件單號」與「手機末 4 碼」，不洩漏身分證或完整個資。
   - **公開安全欄位**：僅揭露單號、物品類型、品牌/型號、地點、日期、處理狀態、維修結果與最後更新時間。
   - **模糊錯誤保護**：查無資料或末 4 碼不符時一律回傳統一提示，杜絕枚舉探測。
3. **服務須知 (`view=guide`)**
   - 完整呈現【志工可修】（電風扇、檯燈、吹風機、玩具、小型家電）、【不可修】（大型家電、高壓設備）、【待現場判定】（微波爐、烤箱、音響）等規範。
   - 清楚載明「7 大無紙化流程」、「零件耗材工本費由民眾自行負擔」及「每人每年度最多登記 5 件，每場限 1 件」等現行規則。
4. **常見問題 FAQ (`view=faq`)**
   - 採用手機友善之 Accordion 手風琴展開卡片。
   - 完整收錄 10 大常見問題（受理類別、費用、零件、送件時間、Email、查詢方式、取件、現場補位、件數上限、修復效益說明）。

---

## 三、 Rich Menu 6 格功能配置與 URI 驗證

本版 Rich Menu 全面採用 **URI Action**，不依賴 Webhook，即使伺服器 Webhook 遇到維護，選單功能依然百分之百暢通：

| 格位 | 功能名稱 | 動作類型 | 完整目標 URI |
| :--- | :--- | :---: | :--- |
| **Area 1 (左上)** | 我要預約 | `uri` | `https://script.google.com/macros/s/.../exec?source=line&view=booking` |
| **Area 2 (中上)** | 維修據點 | `uri` | `https://script.google.com/macros/s/.../exec?source=line&view=locations` |
| **Area 3 (右上)** | 查詢進度 | `uri` | `https://script.google.com/macros/s/.../exec?source=line&view=status` |
| **Area 4 (左下)** | 服務須知 | `uri` | `https://script.google.com/macros/s/.../exec?source=line&view=guide` |
| **Area 5 (中下)** | 常見問題 | `uri` | `https://script.google.com/macros/s/.../exec?source=line&view=faq` |
| **Area 6 (右下)** | 志工招募 | `uri` | `https://forms.gle/WzpEmCJXttoLPD7r7` |

- **安全驗證**：所有 GAS URI 均經由程式強制驗證包含正式 `/exec` 且絕無 `/dev`。
- **圖檔驗證**：已綁定您最新手動更換之 2500 × 1686 圖片（由 GAS 伺服器端串流上傳至 LINE API-DATA）。
- **預設選單**：已由 LINE API 正式設定為全體好友預設選單（Default Rich Menu）。

---

## 四、 路由相容與 Regression Check

1. **Hash Routing 共存驗證**：
   - 既有 `#walkin-EVENT_ID`（現場補位）、`#tv-board`（電視牆看板）等 Hash 路由完整受到保護，優先權高於預設 view。
2. **公開查詢安全 API**：
   - 透過 `searchPublicRepairStatus(serialNum, phoneLast4)` 提供服務端安全查詢，絕不傳遞任何未脫敏個資至前端。
3. **回歸測試結果**：
   - 既有報修登記、場次日曆、電子簽名、管理後台（Mode A/B/C/D）、志工審核、補位功能均運作正常（HTTP 200 OK，HTML 長度完整健全）。
4. **憑證資安守則**：
   - 全程操作未將任何 Token、Secret 或個資寫入本機指令、終端機、暫存檔或報告。
